# State Runtime

Snapshots e artefatos transitórios de estado ficam aqui.

O estado canônico permanece em `kernel/state.json`.

Artefatos importantes para o HITL:
- `state/runtime/hitl-board.json` — feed ao vivo com tarefas abertas, agente ativo, histórico recente e métricas ADK/A2A.
